#pragma once
#include <string>

using namespace std;

// Use the string for each degree program as an index
enum DegreeProgram {SECURITY, NETWORK, SOFTWARE, NONE};
static const string DegreeProgramStrings[] = { "SECURITY", "NETWORK", "SOFTWARE", "NONE"};
